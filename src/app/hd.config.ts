export class HDConfig {
  timeDropdown = {
    startTime : '8:00 AM',
    endTime : '7:00 PM',
    step: 15,
  };
  dateformat = 'yyyy-mm-dd'; 'mm/dd/yyyy';
  disabledDays = [0, 6]; // prime calendar Sunday, Saturday
}
